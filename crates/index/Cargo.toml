[package]
name = "constellation-index"
version.workspace = true
edition.workspace = true

[dependencies]
constellation-extraction = { path = "../extraction" }
constellation-graph = { path = "../graph" }
constellation-linking = { path = "../linking" }
constellation-resolution = { path = "../resolution" }
constellation-store = { path = "../store" }
ignore = "0.4.26"
notify = "8.2.0"
rayon = "1.12.0"
rustc-hash = "2.1.2"
serde = { version = "1.0.228", features = ["derive"] }
thiserror = "2.0.18"
toml = "1.1.2"

[lints]
workspace = true

[dev-dependencies]
tempfile = "3.27.0"
